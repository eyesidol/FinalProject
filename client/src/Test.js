const Test = ({LoginButton, LogoutButton, Profile, Searchbar}) => {

// conditionally render login/logout buttons
    
  return <div>

<LoginButton />
<LogoutButton />
<Searchbar/>
  </div>;
};

export default Test;
